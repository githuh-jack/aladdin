server:
  port: 9200

spring:
  application:
    name: aladdin-auth
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 43.142.17.3:8848
        username: nacos
        password: nacosbw123
        enabled: false
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 1234qwer!
    url: jdbc:mysql://localhost:3306/aladdin?serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull&autoReconnect=true&useSSL=false&failOverReadOnly=false
    username: root
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
  redis:
    host: 61.169.37.106
    password: 1q2w3e4R!@
    port: 9164
    database: 0

mybatis-flex:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mybatis-mapper/**/*.xml
  global-config:
    logic-delete-column: sys005
    normal-value-of-logic-delete: 1
    deleted-value-of-logic-delete: 0

aladdin:
  security:
    enabled: true
    whitelist:
      - /auth/login
      - /auth/logout
      - /auth/userInfo
      - /sso/**
      - /verifyCode/**
      - /css/**
      - /js/**
      - /img/**
      - /doc.html
      - /webjars/**
      - /v3/api-docs/**
      - /swagger-resources/**
      - /actuator/**
    token:
      secret: aladdin-platform-secret-key-2026
      expire-minutes: 30
      header: Authorization
      prefix: "Bearer "
  db:
    enabled: true


